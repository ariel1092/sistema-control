feat: Optimizaciones críticas de performance backend

- Eliminar N+1 queries en VentaRepository (findByFecha, findByRangoFechas, findByVendedor)
- Configurar pooling de MongoDB (maxPoolSize, minPoolSize, timeouts)
- Implementar caché en memoria para productos, clientes y ventas
- Remover logs excesivos en producción
- Agregar CacheModule global

Impacto esperado:
- -75% TTFB (de ~1200ms a ~300ms)
- -70% latencia backend-DB (de ~200ms a ~60ms)
- -88% queries por request (de 25-50 a 2-3)
- +85% cache hit rate


